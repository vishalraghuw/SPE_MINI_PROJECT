# Inventory file defining hosts and their connection settings

# Define a group named "localhost" containing a single host
[localhost]
vboxuser ansible_connection=local

# The host "vboxuser" is part of the "localhost" group
# It uses a local connection for Ansible operations
# This setup allows Ansible to interact with the local machine
